<%= form_for :product, method: :patch, url: product_path(@product.id) do |f| %>
  Name: <%= f.text_field :name %><br />
  Details : <%= f.text_field :details %><br />
  
  <%= submit_tag 'Save' %>
  <%= link_to 'Cancel', product_own_path%>
  <%# <%= f.submit %>
<% end %>